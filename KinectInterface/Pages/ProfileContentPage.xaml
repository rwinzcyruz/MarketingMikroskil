<UserControl x:Class="KinectInterface.Pages.ProfileContentPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:m="http://metro.mahapps.com/winfx/xaml/controls"
        	 mc:Ignorable="d" 
        	 d:DesignWidth="1920" d:DesignHeight="1080"
             Width="1920" Height="1080"
             Background="#7FFFFFFF">
    
    <Grid>
        <DockPanel>
            <Label x:Name="TitleText" DockPanel.Dock="Top" Template="{DynamicResource TitleBarTemplate}">
                <Label.Resources>
                    <VisualBrush x:Key="_icon" Visual="{DynamicResource appbar_home}"/>
                </Label.Resources>
            </Label>

            <Grid x:Name="MainContent" Focusable="True" PreviewKeyUp="Navigate_PreviewKeyUp"></Grid>
        </DockPanel>
    </Grid>
</UserControl>
